<?xml version="1.0"?>
<launch>
    <!-- Face Recognition with Tracker Node -->
    <node name="face_recognizer_tracker" 
          pkg="cv_tracker" 
          type="face_recognizer_with_tracking.py" 
          output="screen"
          respawn="true">
        
        <!-- Параметры для трекера -->
        <param name="tracker_timeout" value="5.0"/>
        <param name="recognition_threshold" value="1.0"/>
        
    </node>
    
    <!-- Monitor Target Messages (optional) -->
    <node name="tracker_monitor" 
          pkg="cv_tracker" 
          type="monitor_tracker_targets.py" 
          output="screen"
          respawn="false">
    </node>
    
    <!-- Display annotated images (optional) -->
    <!--
    <node name="image_view" pkg="image_view" type="image_view" respawn="false" output="screen">
        <remap from="image" to="/annotated_image"/>
        <param name="autosize" value="true"/>
    </node>
    -->
    
</launch> 